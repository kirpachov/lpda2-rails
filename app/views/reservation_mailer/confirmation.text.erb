<%= t('reservation_mailer.greetings', fullname: @reservation.fullname) %>

<%= t('reservation_mailer.reservation_registered', date: l(@reservation.datetime, format: '%e %B %Y'), hour: @reservation.datetime.utc.in_time_zone.strftime('%k:%M'), people: @reservation.people) %>

<% if @reservation.payment %>
  <%= @reservation.payment.paid? ? t('reservation_mailer.confirmation.payment_completed') : t('reservation_mailer.confirmation.remember_payment') %>
<% end %>

<%= t('reservation_mailer.changed_mind') %>

<%= t('reservation_mailer.reservation_details') %>:

<%= @show_reservation_url %>