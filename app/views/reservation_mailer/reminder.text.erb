<%= t('reservation_mailer.greetings', fullname: @reservation.fullname) %>

<%= t('reservation_mailer.reminder.body', date: l(@reservation.datetime, format: '%e %B %Y'), hour: @reservation.datetime.utc.in_time_zone.strftime('%k:%M'), people: @reservation.people) %>


<%= t('reservation_mailer.changed_mind') %>

<%= t('reservation_mailer.reservation_details') %>:

<%= @show_reservation_url %>